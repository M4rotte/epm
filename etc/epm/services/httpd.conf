# Service httpd
# Without any definition the start command is getting the value of $(which httpd) and its desired state is "running".
# In the EPM image, /usr/sbin/httpd is the Busybox HTTP server. Run without argument it will listen on port 80
