version: "3.4"

services:
  epm:
    env_file: .env
    environment:
     - http_proxy
    build:
      context: .
      dockerfile: Dockerfile
      args:
       - http_proxy=${http_proxy}
    image: oxyure/epm:latest
      
